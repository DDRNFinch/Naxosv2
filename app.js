const $=s=>document.querySelector(s);const $$=s=>[...document.querySelectorAll(s)];let taskStructure=null,metadata=null,selectedCourse=null,deferredInstall=null;async function loadJson(path){const r=await fetch(path,{cache:'no-store'});if(!r.ok)throw new Error(`${path}: ${r.status}`);return r.json()}function escapeHtml(v=''){return String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function getStructureCourse(id){return taskStructure.courses.find(c=>c.id===id)}function naturalList(items=[]){if(!items.length)return'';if(items.length===1)return items[0];if(items.length===2)return`${items[0]} and ${items[1]}`;return`${items.slice(0,-1).join(', ')}, and ${items[items.length-1]}`}function renderHome(){selectedCourse=null;$('#backBtn').hidden=true;$('#pageTitle').textContent='Naxosv2';$('#homeView').hidden=false;$('#courseView').hidden=true;const order=['ST0095','ST0264-SITE','ST0264-AJ'];$('#courseCards').innerHTML=order.map(id=>{const m=metadata.courses[id];return`<button class="course-card" data-course="${id}"><div><h2>${escapeHtml(m.title)}</h2><p>${escapeHtml(id)} · v${escapeHtml(m.version)} · Level ${m.level} · ${m.minimumHoursForCompliance} hours</p></div><div class="count">${m.learnerPackCount}</div></button>`}).join('');$$('[data-course]').forEach(b=>b.onclick=()=>openCourse(b.dataset.course))}function openCourse(id){selectedCourse=id;const m=metadata.courses[id];$('#homeView').hidden=true;$('#courseView').hidden=false;$('#backBtn').hidden=false;$('#pageTitle').textContent=m.title;$('#courseHeader').innerHTML=`<h1>${escapeHtml(m.title)}</h1><p>${escapeHtml(m.pathway||m.id)} · official standard v${escapeHtml(m.version)}</p><div class="chips"><span class="chip">${m.learnerPackCount} learner packs</span><span class="chip">${m.ksbCounts.total} KSBs</span><span class="chip">${m.minimumHoursForCompliance} minimum hours</span><span class="chip">${m.typicalDurationMonths} months</span></div>`;setTab('packs')}function setTab(tab){$$('.tab').forEach(b=>b.classList.toggle('active',b.dataset.tab===tab));const m=metadata.courses[selectedCourse],course=getStructureCourse(selectedCourse),out=$('#courseContent');if(tab==='packs'){out.innerHTML=`<div class="pack-list">${course.mainTasks.map((p,i)=>`<article class="pack-card"><h3>${i+1}. ${escapeHtml(p.title)}</h3><p>${escapeHtml(p.summary)}</p><div class="capture"><strong>Capture:</strong> ${escapeHtml(naturalList(p.capture))}.</div></article>`).join('')}</div>`}else if(tab==='standard'){const cells=[['Reference',m.standardId||m.id],['Version',m.version],['Level',m.level],['Status',m.status],['Typical duration',`${m.typicalDurationMonths} months`],['EPA period',`${m.typicalAssessmentPeriodMonths} months`],['Minimum hours',m.minimumHoursForCompliance],['Maximum funding',`£${Number(m.maximumFundingGBP).toLocaleString('en-GB')}`],['LARS code',m.larsCode],['EQA provider',m.eqaProvider],['Knowledge',m.ksbCounts.knowledge],['Skills',m.ksbCounts.skills],['Behaviours',m.ksbCounts.behaviours]];out.innerHTML=`<div class="meta-grid">${cells.map(([a,b])=>`<div class="meta-card"><span>${escapeHtml(a)}</span><strong>${escapeHtml(b)}</strong></div>`).join('')}</div><div class="source-links"><a href="${m.sourceUrl}" target="_blank" rel="noopener">Official Skills England standard ↗</a><a href="${m.epaUrl}" target="_blank" rel="noopener">Official EPA plan ↗</a></div>`}else{out.innerHTML=`<div class="data-box">${escapeHtml(JSON.stringify({metadata:m,packSource:'naxosv2-main-task-structure.json',learnerFacingPackCount:course.mainTasks.length,mappingRule:taskStructure.principles},null,2))}</div>`}window.scrollTo({top:0,behavior:'smooth'})}async function boot(){try{[taskStructure,metadata]=await Promise.all([loadJson('./naxosv2-main-task-structure.json'),loadJson('./data/course-metadata.json')]);renderHome()}catch(err){$('#courseCards').innerHTML=`<div class="error"><strong>Naxosv2 could not load its course data.</strong><br>${escapeHtml(err.message)}</div>`}if('serviceWorker'in navigator){window.addEventListener('load',()=>navigator.serviceWorker.register('./sw.js').catch(()=>{}))}}$('#backBtn').onclick=renderHome;$$('.tab').forEach(b=>b.onclick=()=>setTab(b.dataset.tab));window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredInstall=e;$('#installBtn').hidden=false});$('#installBtn').onclick=async()=>{if(!deferredInstall)return;deferredInstall.prompt();await deferredInstall.userChoice;deferredInstall=null;$('#installBtn').hidden=true};window.addEventListener('appinstalled',()=>{$('#installBtn').hidden=true});boot();